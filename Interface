from tkinter import *

root = Tk()

frame = Frame(root, width=1600, height=800)
frame.pack()

one = Label(frame, text="Quote Calculator")
one.grid(columnspan=7)

entry_1 = Entry(root)
entry_2 = Entry(root)

#COMMAND

start = Button(frame, text="Start", fg="red")
start.grid(row=6)


root.mainloop()
